apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: supply-core-artifacts
  namespace: supply-core
spec:
  accessModes: [ReadWriteOnce]
  # storageClassName: <your-storage-class>  # defaults to the cluster default
  resources:
    requests:
      storage: 1Gi
