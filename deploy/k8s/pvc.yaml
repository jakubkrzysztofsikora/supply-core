apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: supply-core-artifacts
  namespace: supply-core
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
